# Strategy promotion gate — verified mechanics

Two distinct things; do not conflate:
- `src/services/strategyPromotionGate.ts` (171L, pure, fail-closed, `MIN_RANK_SCORE=55`) — runtime ranking/promotion guard. Documented property: it "can only ever NARROW promotion"; never touches live execution, Risk Governor, or DecisionBridge. Preserve that property.
- `scripts/research/runStructuralProfitabilityStudy.mts` (305L) — the sealed-holdout study producing the promotion matrix. Pre-registered `gatePolicy.minHoldoutTrades: 30` (line 39); sample gate `holdoutMetrics.trades >= gatePolicy.minHoldoutTrades` (line 225); `holdoutSealSha256: seal.integrity.contentSha256` (line 264). Data acquisition `scripts/research/acquireProfitabilityData.mts`; QA gate `scripts/qa/verifyProfitabilityStructuralRemediation.mts`.

Sealed-holdout machinery exists ONLY under `scripts/research/**`. Verified: `minHoldoutTrades|holdoutSeal|sealedHoldout` = 0 hits in `src/**/*.ts`. Nothing in `src/` can burn a holdout.

NOT in the release pipeline: no `profitability` / `research:` entry in the package.json `verify` chain, so study failures never block `npm run verify` or `release:package`. Standalone research, run explicitly.

## The nine gate flags
`sample, return, profitFactor, drawdown, costStress, fullStrategySemantics, riskPolicy, browserQa, distinctTradeSequence` — all present flags must pass to promote.

Correction (2026-08-23): only the 5 native-scanner rows actually carry nine flags. `distinctTradeSequence` is attached after the main loop for `nativeScannerIds` only (study script :245-258), so the other rows carry eight and `Object.values(gates).every(Boolean)` never requires the ninth. The sealed artifact contains exactly these two key shapes.

## Previously-assumed nearest candidate: `news-sentiment-momentum-breakout-v1` on `BTC_1H` ("BTC 1h news momentum")

Superseded 2026-08-23: by gate distance the nearest row is BTC_4H `volatility-squeeze-trend-volume-expansion-v1` (32 holdout trades, PF 1.429, fails only `fullStrategySemantics` + `browserQa`). The mechanics below remain correct for the news-momentum row, which is strictly further away. Matrix scope (26 rows = 10 strategies x 3 datasets minus 4), the two 26/26 universal blockers, and the corrected ranking: `mem:strategy/profitability_roadmap`.
Sealed artifact `QA/profitability-structural-remediation/structural-profitability-results.json`: holdout {trades 19, net +1.326%, PF 1.3485 (line 906), DD 1.262%}; costStress {net +0.467%, PF 1.1125}; native {19 trades, +3.064%, PF 1.8079}; holdout sequence hash `c7505dbaefd781f9`. Seal chain consistent: `results.holdoutSealSha256` = `holdout-seal.json.integrity.contentSha256` = report §6. **Treat the seal SHA as the burn detector** — if it ever changes, the holdout was re-run and is no longer the sealed one.
Report: `Doc/reports/final/APEX_STRUCTURAL_PROFITABILITY_REMEDIATION_2026-08-22.md`, verdict "NOT YET PROFITABLE OR PROMOTABLE".

REJECT is driven by three flags (Return / PF / DD / costStress / distinctTradeSequence all PASS):
1. `sample:false` — 19 holdout trades < pre-registered 30. Guardrail, not a bug. The strategy is selective **by design**: `strategyEngine/signalAwareScannerAdapter.ts` ~line 107 iterates real Google-News RSS headlines and requires `headlineTone(news)` × 12h price-momentum sign agreement × `momentum >= 0.4%` × above-20SMA volume, else `reject('news_price_volume_confirmation')`. Loosening entry to manufacture >=30 trades = overfitting + gate-lowering. Only new sealed data can clear this.
2. `fullStrategySemantics:false` — intentional guardrail, NOT a defect. `strategyValidationCapability()` (`strategyRegistry.ts:569`) returns `BASE_REPLAY` whenever any fusion component with `weight>0` has `dataMode==='LIVE_ONLY'`. This strategy weights five such components (news 0.18, sentiment 0.14, openInterest 0.07, whaleFlow 0.05, funding 0.04); `FUSION_COMPONENTS` (registry ~line 33) marks all five LIVE_ONLY; `fusionBlueprint()` spreads base `dataMode`+`label` onto each weighted component, emitting the blocker string. `semanticBlockers[0]` verbatim in the sealed artifact: "Historical validation does not bind timestamp-aligned LIVE_ONLY fusion evidence: Funding and crowding, Open-interest state, Market sentiment, News event context, Whale exchange flow." `dataMode` is a GLOBAL per-component property shared by all 10 core strategies — flipping it changes many pairs (user forbade) and the report's interpretation constraints explicitly deny historical proxies as live evidence (RSS != newswire, fear-greed != model sentiment, whale entity-flow Unavailable). Leave untouched.
3. `browserQa:false` — **tooling blocker RESOLVED 2026-08-23; gate now runs on Windows but legitimately FAILS.** See the browserQa section below for the full verified state. Still `false` in the sealed artifact, because the study was deliberately NOT re-run (seal preserved).

## browserQa — diagnosed and tooling-fixed 2026-08-23 (Windows)

**The old NOT_RUN record was misattributed to this host.** It never described a Windows failure:
- The producer is `scripts/qa/runOfflinePixelGate.mjs` (writes `pixel-qa.json` at :104). It hardcodes `status: 'completed'` and emits only `status/generatedAt/browser/testedBuild/viewport/mode/runtimeGatePassed/rootTextLength/pageErrors/consoleErrors/pixelPassed/pixelTotal/results/passed`. The old artifact's `status: "blocked_before_test"` + `installation`/`launchAttempts`/`cloudBrowserAttempt`/`verdict`/`reason` are fields **this script cannot emit**; `blocked_before_test` = 0 hits in `scripts/**`. It was a hand-authored incident report, not runner output (honest, but not machine evidence).
- Its sibling `browser/offline-index.html` carried `<base href="file:///workspace/scratch/193397e54c13/project/dist/">` — a **Linux container path**. The generating line (:18 `pathToFileURL`) emits `file:///C:/...` on Windows. The failing attempt ran in a Linux sandbox.
- Refuted empirically on Windows: `chromium.launch({headless:true})` → `LAUNCH_OK version=151.0.7922.34`, **both** with and without explicit `executablePath` — the same version the artifact claimed could not start. So chrome-151 SIGTRAP/133, headless-shell SIGSEGV/139 and `failed_tls_proxy_certificate_not_yet_valid` are sandbox/proxy artifacts. **Nothing needs downloading**; `chromium_headless_shell-1234` is already installed under `%LOCALAPPDATA%\ms-playwright`. Do not re-diagnose these as environment limits here.

**Two real defects fixed in `runOfflinePixelGate.mjs` (no gate weakened):**
1. `spawnSync('python3', ...)` (was :70) — on Windows `python3` is a **0-byte Microsoft Store App Execution Alias stub**: `python3 --version` → "Python was not found…", **exit 9009**. Unfixed, all 8 routes get `diffExitCode 9009` → `verdict:'tool_failed'` → a tooling failure disguised as a visual failure. Now resolves an interpreter: honors `APEX_PYTHON`, else probes `python3`/`python`/`py -3` and requires exit 0 + `/^Python 3/`; throws if none. Working interpreter here is `python` → 3.13.7 (numpy/PIL/scipy present; `apex_visual_diff.py` self-diff = `verdict pass, MS-SSIM 1.000, edge F1 1.000`).
2. Report metadata hardcoded `version: 'Google Chrome for Testing 151.0.7922.34'` + `archiveSha256: 'ae8736ac…'` — a false provenance claim once Playwright's own build is used. Now reports real `browser.version()` plus a `visualDiff: { interpreter, tool }` block.
- `APEX_PLAYWRIGHT_EXECUTABLE` is still hard-required (:7-8) — left alone deliberately, satisfiable at invocation. Contrast `verifyUi1368.mjs` :23-24/:567, which makes it optional (its `SYSTEM_CHROMIUM` fallback lists Linux paths only, so on Windows it falls through to Playwright's own browser).

**Invocation that works (no npm script exists for this runner — it is standalone research tooling, not in the `verify` chain):**
`$env:APEX_PLAYWRIGHT_EXECUTABLE='<…>\chromium_headless_shell-1234\chrome-headless-shell-win64\chrome-headless-shell.exe'; node scripts/qa/runOfflinePixelGate.mjs`

**Real verified result (2026-08-23T18:49:24Z, testedBuild `4b45b635-3a1670c3` / sourceHash `3a1670c304bd`, dirtyTree true):**
`status: completed`, `runtimeGatePassed: true`, `rootTextLength: 224`, `pageErrors: []`, **`pixelPassed: 0` / `pixelTotal: 8`**, `passed: false`. All 8 `diffExitCode: 0`, `diffStderr: ""` — **the comparisons genuinely executed (0/0 → 0/8)**. That is the tooling fix, verified.

**But the gate fails for a NEW substantive reason — the app shell crashes under the offline harness.** All 8 routes report `verdict: "review"`, msSsim 0.685–0.738, `edgeF1` **0.027–0.056**, pctPixelsChanged 5.4–8.6%. Near-zero edge agreement at only ~6% pixel change = near-empty capture. Confirmed: **all 8 captures are byte-identical at 19,706 bytes** (references are 726–944 KB) and render `RouteErrorBoundary.tsx` ("Workspace recovery / This workspace could not be rendered", diagnosticsId `ui-mt65uyo9-fak9um`). `consoleErrors` carry `TypeError: Cannot read properties of undefined (reading 'status')` + `[APEX] Route render failure {route: application shell}` and 9× `ERR_FILE_NOT_FOUND`.
- Leading hypothesis (**unverified this session**): harness-stub incompleteness, not an app defect. The runner's `window.fetch` stub returns `{ok:true,dataState:'degraded',timestamp}` for any URL outside its four regexes (tickers|majors|markets|scanner / portfolio|account|balance / orders / strateg), supplying no `status` field. `qa:ui-1368` serves LIVE source via vite with real endpoints and passed on 2026-08-19 — but on an older build, so attribution is not proven. **Do not "fix" this by enriching the stub — that is manufacturing fake data.**

**GATE-INTEGRITY DEFECT — false PASS, do not trust `runtimeGatePassed` from this runner.** Criterion (was :87) is `rootTextLength >= 40 && pageErrors.length === 0`. `RouteErrorBoundary` **catches** the React throw, so it surfaces as a *console* error and `pageErrors` stays empty; the recovery screen's own text is 224 chars ≥ 40. So a totally unrendered app reports `runtimeGatePassed: true`, and `process.exitCode = runtimeGatePassed ? 0 : 1` makes the process **exit 0**. Same family as the `qa:ui-1368` blind spot in the user's visual-defects notes. NOT fixed — tightening it is a semantic gate rewrite and needs an explicit decision.

Consequence: **no honest code change makes this candidate promotable.** Even a passing browser QA leaves `sample` and `fullStrategySemantics` false. If asked to "make it pass", push back — that would require faking trades or relabelling proxy data.

## Honestly-labelled data gaps (not stubs, do not silently upgrade)
Top-of-book spread: Binance bookDepth archives lack top-of-book bid/ask, and the bookTicker archive ends 2023 so it does not cover the sealed 2024-25 holdout window. Entity-classified whale flow: Whale Alert rejects unauthenticated calls, no credentials supplied. Binance top-trader/taker data is used only as an explicitly labelled proxy.

## Subsystem is complete — do not re-audit for stubs
Zero TODO/stub/mock/placeholder markers across `strategyRegistry.ts`, `strategyEngine/signalAwareScannerAdapter.ts` (5 native-signal scanner strategies, fail-closed, no candle-proxy fallback), `riskGovernor.ts` (340L; <=0.75%/trade, 35% gross cap, 8% throttle, 12% hard shutdown), `strategyPromotionGate.ts`, `paperForwardEvaluator.ts` (635L), `strategyValidation.ts`. Any brief premised on "implement the unimplemented strategy parts" is working from a false premise.

## Known artifact drift
On-disk `runStructuralProfitabilityStudy.mts` iteration-5 text ("Ran real Chromium gate and reference pixel comparisons") and its 9-column promotion table no longer match the committed 2026-08-22 report (iteration-5 "launch remained blocked... NOT_RUN"; 10-column table including Browser). The committed report came from a newer generator than the script now on disk — regenerating from the current script will not reproduce it.
