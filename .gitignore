# Dependencies
node_modules/

# Build output
dist/
_release/
dist_stale_backup_*/
build/
coverage/

# Archived build backups / non-source snapshots
_archive/

# QA evidence (regenerated by npm scripts)
_qa/
/qa/
/QA/
Print-Screen/
Archived/
# Playwright/visual run output. Regenerated by test:visual and qa:workspace-runtime;
# every writer mkdirs its own directory, so a fresh checkout without this is fine.
test-results/

# Local source/tooling archives. These are snapshots of the repository itself,
# not inputs to npm ci or the build, and tools.zip alone is 91 MB.
/*.zip
# Snapshot archives that land inside the source tree (e.g. src/services.zip, a 647 KB
# duplicate of src/services/). Scoped to src/ on purpose: vendor/*.tgz are deliberately
# tracked lockfile inputs and must not be caught by a blanket archive rule.
/src/**/*.zip

# Local browser binaries for the visual gates. Playwright is installed from the
# lockfile in CI, so these must not be versioned.
/chrome/
/chromedriver/
.playwright-browsers/

# Working notes that live at the repo root during a task, not shipped source.
/AUDIT_FIX_PLAN.md
/Claude Code Prompt — Implement APEX Screener Page.md

# Env / secrets
.env
.env.local
.env.txt
*.config.json
!*.config.example.json
.telegram*
.supplemental*
.external-api-sources*
!.external-api-sources.config.example.json
# Owner-provided provider credential sheet. Read at runtime only; never committed.
api-config*.txt
# Token/credential sheet variants. `.env.txt` is covered above; this catches the
# undotted form so neither spelling can ever be staged.
env.txt

# Local databases. No SQLite file is expected in this tree; these patterns exist so
# one can never be committed by accident.
*.db
*.sqlite
*.sqlite3

# Local state / logs
.apex-data/
temp/
.tmp-*-store-*.json
*.log
dev_log.txt
npm-debug.log*

# Editor / agent tooling
.cursor/
.vscode/
.kiro/
.agent-index/
.claude/
.kilo/
.postman.json
.mcp-recovered/

# OS
.DS_Store
Thumbs.db
# APEX Project Intelligence Hub local outputs
.apex-index/
package.json.apex-hub.bak

# Regenerated documentation indexes and local visual report
Doc/FUNCTION_INDEX.md
Doc/FUNCTION_INDEX.json
Doc/DOCUMENTATION_INDEX.md
Doc/DOCUMENTATION_INDEX.json
Doc/generated/APEX_COMPLETE_VISUAL_PROJECT_DOCUMENTATION.html

# Deterministic generated indexes are committed so CI can enforce drift.
!Doc/FUNCTION_INDEX.md
!Doc/FUNCTION_INDEX.json
!Doc/DOCUMENTATION_INDEX.md
!Doc/DOCUMENTATION_INDEX.json
